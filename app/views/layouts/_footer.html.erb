<footer class="d-flex flex-column h-100">
	<div class="input-group mb-4 p-3">
		<%= form_with url: subscriptions_search_path, method: :get do |f| %>
			<%= f.text_field :keyword, id: "search", placeholder: "サブスク検索", class: "form-control" %>
		<% end %>
		<label for="search" class="btn btn-primary">
			<i class="fas fa-search"></i>
		</label>
	</div>
	<div>
		<h3>人気のサブスク</h3>
		<% @top_subscriptions.each do |subsc| %>
			<%= render 'public/subscriptions/card_horizontal', subsc: subsc %>
		<% end %>
	</div>
	<div class="d-flex justify-content-center mt-auto">
		<p>CopyRight Sota Aoyagi</p>
	</div>
</footer>